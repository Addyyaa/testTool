# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['version_publisher\\version_publisher.py'],
    pathex=[],
    binaries=[],
    datas=[('version_publisher/asserts/user.env', 'version_publisher/asserts')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='version_publisher',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='version_publisher/asserts/publisher.ico'
)
